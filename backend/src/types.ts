export type Student = {
  id: string;
  admission_number: string;
  name: string;
  active: boolean;
  created_at: string;
};

export type Teacher = {
  id: string;
  teacher_id: string;
  name: string;
  active: boolean;
  created_at: string;
};
